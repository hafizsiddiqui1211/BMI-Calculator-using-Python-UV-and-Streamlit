[project]
name = "bmi-calculator"
version = "0.1.0"
description = "BMI Calculator using Streamlit"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit"
]

[tool.poetry]
package-mode = false
